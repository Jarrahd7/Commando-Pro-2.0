## Packages
framer-motion | Essential for smooth animations and transitions (timer, buttons)
lucide-react | Iconography (already in base but confirming usage)
clsx | Utility for conditional classes (already in base)
tailwind-merge | Utility for merging tailwind classes (already in base)

## Notes
Audio files are served statically from `/audio/`.
Backend provides `/api/commands` listing available commands.
No extra auth or complex integrations needed.
